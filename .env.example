# Windows example environment file for the template
# All keys are optional. Values are placeholders.
# Keep this file in English only and ASCII-safe.

# Observability / Sentry
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_RELEASE=0.1.0

# SonarCloud (optional in CI; steps should be conditional on these)
SONAR_TOKEN=
SONAR_ORG=
SONAR_PROJECT_KEY=

# Feature flags
FEATURE_EXPERIMENTAL=false

# Playwright configuration (override in CI if needed)
PWTEST_WORKERS=1

# Pack smoke: set to 1 to run build before checks
SMOKE_PACK_BUILD=0

